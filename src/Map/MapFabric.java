package Map;

public class MapFabric {
    public Path createPath(Object block) {
        if (block == Block.BLOCK || block == Block.COIN_BLOCK || block == Block.POWERUP_BLOCK) {
            return new Path(block.)
        }
    }
}
